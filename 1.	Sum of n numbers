#include <stdio.h>

int sumOfN(int n) {
    if (n == 0)
        return 0;
    else
        return n + sumOfN(n - 1);
}

int main() {
    int n;
    printf("Enter a number: ");
    scanf("%d", &n);
    printf("Sum of first %d numbers = %d\n", n, sumOfN(n));
    return 0;
}	
